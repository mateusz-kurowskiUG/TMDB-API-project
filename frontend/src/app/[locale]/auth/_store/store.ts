// import { create } from "zustand";

// interface AuthFormStoreProps {
//   authFormType: "sign-in" | "sign-up";
// }

// export const authFormStore = create<AuthFormStoreProps>((set) => ({
//   authFormType: "sign-in",
// }));
